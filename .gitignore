*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build/
/app/build/
/captures/
.externalNativeBuild/
.cxx/

# Signing material, if release signing is ever set up — never commit these
*.jks
*.keystore
!/app/debug.keystore
keystore.properties

# Session work files — never commit these
prompt.md
cleanup.md
design.md
